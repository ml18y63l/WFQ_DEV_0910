# 对 Spec_Review_Comment_0911.md 的分析

| 项目 | 内容 |
| :--- | :--- |
| 日期 | 2026-09-11 |
| 评审建议 | [Spec_Review_Comment_0911.md](Spec_Review_Comment_0911.md) |
| 对照规格 | [Design_Spec_V1.md](Design_Spec_V1.md) |
| 分析范围 | 判断建议是否成立、修订影响和优先级；本文不修改 V1 |
| 方法 | 对照现有条文；独立复核争议公式、边沿语义及可达冲突实例 |

## 1. 结论

评审总体有参考价值。故障注入约定、参数公式、事务级一致性断言、验证工具基线和定向覆盖等建议有助于后续 RTL 落地。

但不建议按原文全部采纳：

1. **G-3 的 Trie RAM 深度公式错误，多乘了一个分支因子 16。**
2. **E-2 混淆了 E1 沿后响应有效与 E2 沿最早握手；直接照改可能额外增加一拍。**
3. **I-1 所要求的初始化边沿顺序，V1 第 12.2 节已明确写出，不能认定为尚未定义的重要缺口。**
4. **G-2/R-1 中“两个比较串行”和“配合约束可闭合”的表述不准确。**
5. **第 8 节状态断言建议把条件写宽了，并错误暗示 empty 不应采用组合比较。**
6. **吞吐数字算术成立，但“线速”“系统瓶颈”和“512 B 不构成约束”的判断缺少系统条件。**

据此，可以保留“核心算法未因本轮建议而被推翻、可启动功能 RTL”的判断，但不能把文档复算等同于 RAM 宏适配、RTL 正确性或目标频点已经得到验证。

## 2. 逐项处置建议

| 编号 | 判断 | 建议处置 |
| :--- | :--- | :--- |
| I-1 | 部分合理，严重性偏高 | 增加沿前/沿后表有帮助，但现有顺序已明确，应降为表述澄清 |
| I-2 | 合理，但“不可执行”过强 | 补 TB 注入约定、检测点、预期 fault_code 和恢复流程；不必增加硬件注错接口 |
| I-3 | 合理的范围澄清，条件性重要 | 在 ASIC wrapper 定型前明确 DFT 归属；不能据此直接要求功能核心增加 bist_mode |
| G-1 | 有价值，需限定 exact 的含义 | 使用整 tag 的 trie_exact，与同一事务保存的 old_rc 比较 |
| G-2 | 化简成立，时序描述需修正 | 两个等值比较并行计算；只在合法 marker 不变量下具有所述等价性 |
| G-3 | 建议目的正确，公式需纠正 | 分开列出 RAM 条目数、位宽和副本数；RC 用 COUNT_WIDTH 表达 |
| G-4 | 可采纳，部分内容已存在 | 明确组合路径和上游 valid 责任；寄存隔离按目标互联要求决定 |
| G-5 | 可采纳，低风险完善 | 空队列忽略 base_epoch；第一条插入重建，非空才检查窗口关系 |
| G-6 | 部分采纳 | 不承诺违规输入后的功能结果；不要一概禁止验证协议 checker 的负向测试 |
| G-7 | 合理 | 在实施/验证计划中固定语言子集、仿真器、lint 和注入能力 |
| E-1 | 合理 | 明确“时序表”是 V1 第 10 节周期表 |
| E-2 | 不按原文采纳 | 区分响应入 FIFO、沿后信号有效、下游采样消费三个时刻 |
| E-3 | 合理但已有说明 | 可再补一句设计意图，不构成状态语义缺失 |
| E-4 | 冗余的编辑建议 | 附录 C 已列乘积和计数规模；可增加“每组检查什么”，优先级低 |

## 3. 必须纠正的公式和时序解释

### 3.1 G-3：RAM 深度应为分支因子的 level-1 次方

V1 使用 Level 1～3，根层每个 bank 只有一个节点。正确公式为：

~~~text
B = BRANCHING_FACTOR
DEPTH_L(level) = EPOCH_BANKS * B**(level - 1)
WIDTH_L(level) = B
BITS_L(level)  = DEPTH_L(level) * WIDTH_L(level)
~~~

默认配置：

| 层级 | 正确逻辑深度 | 每节点位宽 | 评审公式给出的错误深度 |
| :--- | ---: | ---: | ---: |
| L1 | 2 | 16 | 32 |
| L2 | 32 | 16 | 512 |
| L3 | 512 | 16 | 8192 |

评审中的 `EPOCH_BANKS × 16^level` 是该层**不含副本的总位数**，不能作为 RAM 地址深度。物理副本还应单独按 L1×1、L2×2、L3×3 计入。

其余通用公式建议写为：

~~~text
TT_WIDTH   = 1 + PTR_WIDTH
NEXT_WIDTH = 1 + PTR_WIDTH
RC_WIDTH   = COUNT_WIDTH = $clog2(MEM_DEPTH + 1)
DATA_WIDTH = EPOCH_WIDTH + TAG_WIDTH + FLOW_ID_WIDTH
FREE_WIDTH = PTR_WIDTH
TT_DEPTH = RC_DEPTH = EPOCH_BANKS * 2**TAG_WIDTH
DATA_DEPTH = NEXT_DEPTH = FREE_DEPTH = MEM_DEPTH
~~~

在 V1 的 `MEM_DEPTH=2**PTR_WIDTH` 约束下，RC_WIDTH 确实等于 PTR_WIDTH+1；保留容量公式更能反映计数器的真实含义。

### 3.2 E-2：响应有效时刻不等于消费时刻

假定响应 FIFO 原为空且下游一直 ready，按 V1 的寄存提交规则：

| 时刻 | 行为 |
| :--- | :--- |
| E0 上升沿 | extract_fire，接受请求 |
| E1 上升沿 | 逻辑提交、响应入 FIFO |
| E1 沿后 | 寄存器更新完成，extract_val=1，响应数据有效 |
| E2 上升沿 | 下游最早采样并完成该响应的握手 |

所以正确表述是：

> 请求在 E0 接受，E1 沿提交并将响应写入 FIFO。若此前无更早响应，extract_val 和数据在 E1 沿后有效；若下游 ready，最早在 E2 沿消费。

评审建议“自 E2 起对外有效”若指 E2 沿后，便是额外一拍输出延迟；若指 E2 采样，则应写“消费”，不能写“开始有效”。

FIFO 已有更早响应时，新响应何时成为外部 FIFO 头取决于之前响应的消费。此时也不能无条件保证新响应在 E2 对外出现。

SVA 还需区分上升沿采样值与 NBA 后更新值，不能单凭信号在波形中标为 E1 就机械套用一个延迟操作符。

### 3.3 G-2/R-1：比较可并行，但不能推导 STA 必然闭合

在合法节点满足“被删除的 marker 原先为 1”的条件下：

~~~systemverilog
leaf_only   = (old_leaf   == (16'h0001 << c));
parent_only = (old_parent == (16'h0001 << b));

WE_L3 = last_of_key;
WE_L2 = last_of_key & leaf_only;
WE_L1 = last_of_key & leaf_only & parent_only;
~~~

leaf_only 和 parent_only 分别依赖两个已经并行读出的旧位图，**不存在一个等值比较等待另一个比较结果的必要依赖**。更准确的逻辑结构是并行比较加控制归约，再驱动各写使能。

这是一项值得采纳的实现优化，但需保留三个限制：

- 物理门级深度由综合映射决定，不能把“两个 16-bit 比较”直接当作两个门延迟。
- 出队关键路径还可能包括 RAM q、转发选择、RC 运算、bank/head/cache 更新、故障阻断、FIFO 控制和写口建立时间。
- 在未确定宏、工艺角、负载和布局并完成 STA 前，不能声称“配合约束可闭合”。

等价性也不是针对任意损坏状态成立。例如 old_leaf=0、c=0 时，原式 new_leaf==0 为真，而 old_leaf==onehot(c) 为假。故障注入环境要明确：正常数据路径优化依赖不变量，损坏状态由既定故障检测/停止契约处理。

### 3.4 I-1：初始化时序已有定义

V1 第 12.2 节已写明：

~~~text
首写：R0
末写：R_(INIT_WRITES-1)
guard：R_INIT_WRITES
首次允许接受：R_(INIT_WRITES+1)
~~~

因此默认 R8192 握手的解释与现有条款相违背，不是另一种同样符合 V1 的实现。

可补强的内容是：

- init_done 在 guard 沿寄存置位。
- ready 使用已寄存的 init_done，不使用“即将完成”的 next-state 提前放行。
- 默认从 R0 到 guard 的沿间距离为 8192T；从 R0 到首次允许接受 R8193 为 8193T。
- “8193 个时钟预算”的终点应明确为首次允许接受，外部复位释放延迟另计。

不能笼统禁止 init_done 到 ready 的组合门控路径；应禁止的是越过 guard 的提前前馈。

## 4. 合理建议的适用边界

### 4.1 I-2：补故障注入方法，保留功能接口

F24 已规定了要验证的错误响应，TB 可用后门或 force/release 实现，所以“目前不可执行”过于绝对。实际缺口是团队未统一可移植的注入方法。

建议在验证计划中定义：

| 注入对象 | 注入位置与检查 |
| :--- | :--- |
| RC | 在目标事务有效读回之前破坏计数，触发对应的上下溢检测 |
| TT.valid | 构造 Trie 前驱有效且 TT 无效，在前驱校验阶段检测 |
| head_cache/必要链路 | 构造计数与有效指针矛盾，在出队提交前检测 |
| 容量/响应计数器 | 检查计数越界 fault，避免误当正常背压 |

注入必须说明时刻及作用对象是 RAM、wrapper 读回值、overlay 还是事务快照。只修改 RAM 可能被有效 overlay 遮蔽，从而根本没有触发目标故障。

恢复要求沿用 V1：阻止有问题事务的逻辑提交，已提交响应可以继续消费，然后复位并完成初始化。多个错误同拍出现时还可补 fault_code 优先级，使检查结果确定。

建议使用 TB 适配层，不在生产接口增加永久注错端口。具体后门方式需随 G-7 的工具选择确认。

### 4.2 I-3：明确 DFT 归属，不急于设计 BIST 端口

明确 DFT/MBIST/scan/repair 范围是合理要求，特别是在目标 SRAM wrapper 即将定型时。

但 V1 第 6.2 节说的是**容量估算不包含 ECC 等额外开销**，不能据此推导出完整的 ECC/DFT 功能排除声明。

评审给出的“FPGA 基线不做 DFT”与“核心预留 bist_mode”也不是仅有的两种选择。功能 RTL 可以继续面向 ASIC/FPGA，DFT 则由 SoC 集成层或技术相关 macro wrapper 承担，保持功能端口契约。

在 ASIC 测试流程尚未确定时，应先写清归属及正常模式下延迟/端口不变的要求；没有依据提前冻结通用 bist_mode 的极性、时序和仲裁。

### 4.3 G-1：断言应比较同一事务的整 tag 命中

建议使用：

~~~text
trie_exact =
    pred_same_valid && (pred_same_tag == transaction_tag)

assert(trie_exact == (transaction_old_rc != 0))
~~~

这里的 old_rc 是该事务 E0 发读、随后保存到 E3 的值。不能把某个父层 matcher 的 literal exact 直接等同于完整 tag 存在，也不能比较当前接口 tag 的 RC。

V1 已有 RC/TT/叶 marker 的结构等价断言；新建议的价值在于增加**跨流水通路的事务级对照**，而不是此前完全没有一致性检查。

### 4.4 G-4/G-5/G-6/G-7

- **G-4：** V1 已允许 ready 组合依赖 valid，并要求上游不等待 ready 才发 valid。可以补互联责任说明，但是否需要 skid buffer/寄存切片应根据目标协议、组合环及时序要求确定，不应无条件强制。
- **G-5：** 完全为空时 base_epoch 可以保留旧值或写确定值，但不得用于拒绝首条任意 epoch；窗口检查应限定非空。保留旧值有利于波形阅读，不必将 RTL 值驱成 X。
- **G-6：** DUT 不需要保证协议违规后的功能结果，但仍可用负向激励验证环境中的协议断言能正确报错。“不保证恢复”不等于“禁止相关负向测试”。
- **G-7：** 固定语言版本、仿真器及注入/断言支持能力合理。可放入实施计划；不必为了完善功能规格而先绑定某个商业工具或强制 UVM。

## 5. 风险及补充测试建议

### 5.1 R-2：包速率正确，线速判断需限定口径

在 f=300 MHz、均衡插入/出队的假设下：

~~~text
P = f/16 = 18.75 Mpacket/s
64 B：P * 64 * 8 = 9.6 Gbit/s
512 B：P * 512 * 8 = 76.8 Gbit/s
~~~

这两项是按给定包字节数计算的位率。物理链路还可能有该包长口径未计入的额外开销；若每包另有 H 字节，则对应的链路需求为 P×8×(B+H)。

因此可采纳数值示例，但需删去没有目标链路、包长口径和业务负载支撑的“必然成为瓶颈”及“512 B 不构成约束”。系统判断应比较所需 Mpacket/s 与可服务 Mpacket/s。

### 5.2 同址 RAW 示例确实可达，应明确加入 F22

评审第 7 节给出的“在第二节点之后插入”判断成立。可用下例直接落成顶层用例：

~~~text
初始：A(tag=10) -> B(tag=20) -> C(tag=30)
T0 插入 N(tag=25)，前驱为 B。

T0 E7：
  逻辑链表成为 A -> B -> N -> C
  物理 NEXT[N]=C
  head_cache.next 仍为 B

T0 E8 / T1 E0：
  T0 写 NEXT[B]=N
  T1 接受出队 A，为缓存下一头 B 而读取 NEXT[B]
  同拍、同址发生读写

T1 E1：
  新 head_cache 必须为 B，其 next 必须是 N，不能是旧值 C。
~~~

这里实际命中的是 T0 **先前已经逻辑提交、尚未退休的 overlay**；它并非在 E8 新发生一次逻辑提交。V1 的旁路优先级覆盖该情形，但评审“本沿新提交写集合优先”的解释应修正。

可以同时保留 RAM wrapper 的同址/异址单元测试，但不应只做单元测试而遗漏这个合法的顶层场景。

### 5.3 第 8 节测试建议逐项判断

| 建议 | 判断和修正 |
| :--- | :--- |
| F04 满容量后完整排空 | 采纳，特别检查 4096 个同值项逐项出队、最终 RC/marker/TT 清零及地址全部归还 |
| U-RAM 同址/异址测试编号 | 采纳；V1 已要求相关测试，补编号和与 F22 的追踪关系有帮助 |
| epoch 跳到 65535 邻域、bank 重用至少三次 | 采纳，但跳变只能在复位/空队列重建时合法设置，或通过合法连续推进实现，不能在非空窗口随意跳代 |
| 插入待提交期间 queue_level 不变 | 在当前发射结构、无复位/fault条件下成立；验收应比较已寄存的已提交状态 |
| 插入待提交期间 empty=1 | 仅当接受前已为空时成立；若原队列非空，应继续为 0 |
| 防止 empty 接组合比较 | 不采纳这句话。`empty = (queue_level == 0)` 的组合比较符合 V1；错误在于比较了含预留的错误计数源 |

也建议将容量、RC 总和等全局不变量明确限定在 init_done 且无 fault 的正常工作区间。初始化扫描过程不能机械要求尚未建立的完整队列不变量成立。

## 6. 建议的采纳顺序

1. **先纠正评审文字：** G-3 深度公式、E-2 边沿语义、G-2/R-1 比较路径与时序保证、第 8 节状态断言、R-2 的线速口径。
2. **在 RTL/验证启动前补充：** F24 注入与检查约定、DFT 职责边界、语言和工具能力基线、明确的顶层 RAW 用例。
3. **随规格维护完善：** 参数公式表、trie_exact 断言、初始化沿表、空队列 base 语义、满容量排空测试。
4. **保持现有核心契约：** 8 拍发射、插入 7 拍提交、出队 1 拍提交；当前评审没有提供足以改变这些契约的综合或系统吞吐证据。

不建议把“三项重要问题预计半天完成”当成确定工期。DFT 归属和工具链若涉及外部流程，耗时取决于集成信息；若仅补清晰的范围声明，则修改可以很小。

## 7. 本轮独立复核记录

本轮采用临时 Python 计算，针对争议点重新核对：

- 三层 RAM 条目数：2、32、512；确认评审深度公式偏大 16 倍。
- 对全部 16-bit 位图和每个已置位位置，共 524,288 个合法组合，验证 `(old & ~mask)==0` 与 `old==mask` 等价。
- 构造 old=0 的反例，确认上述等价依赖 marker 有效这一前提。
- 用寄存 FIFO 沿前/沿后模型确认 E1 沿后 valid、E2 最早消费。
- 构造 A→B→C、插入 N 的场景，确认顶层 NEXT[B] 同址 RAW 可达。
- 复核 f/16 和包字节数换算。

评审报告声称的其余独立复算脚本未包含在本次提供的评审 Markdown 内。其结果可以作为报告证据阅读，但不能视为本轮重新执行过的项目。建议后续把规格模型和运行命令纳入可复现的验证资料；这些逻辑检查仍不替代 RTL、RAM 宏及 STA 验证。
